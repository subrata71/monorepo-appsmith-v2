// Dummy declarations for scaffold tokens.
// They are replaced with real names when you copy a template
// (user, users, User, NewUser, …). Until then they reference the template schema types.

import type { __Entity__, New__Entity__, __entityPlural__ } from '../db/schema';

export { __entityPlural__ };
export type { __Entity__, New__Entity__ };
