/**
 * Add Note Feature API Mutations
 *
 * Re-exports the note creation mutation from entities layer
 * and provides any feature-specific mutation logic.
 */

export { useCreateNote } from '@/entities/note';
