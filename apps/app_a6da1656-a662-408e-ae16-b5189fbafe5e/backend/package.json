{
  "name": "@app/backend",
  "version": "0.0.1",
  "description": "",
  "type": "module",
  "main": "index.js",
  "engines": {
    "node": "~22.17.1",
    "pnpm": "~10.14.0"
  },
  "scripts": {
    "start": "node --import tsx src/server.ts",
    "dev": "node --import tsx --watch src/server.ts",
    "test": "NODE_OPTIONS=\"--import tsx\" npm run test:setup && vitest run",
    "test:watch": "NODE_OPTIONS=\"--import tsx\" npm run test:setup && vitest",
    "test:setup": "node --import tsx scripts/test-setup.js",
    "coverage": "vitest run --coverage",
    "typecheck": "tsc -p tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "prettier": "prettier --check .",
    "preinstall": "npx only-allow pnpm"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,css,md,json,html}": [
      "prettier --write"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@app/shared": "workspace:*",
    "@types/pg": "^8.15.4",
    "@vitest/coverage-istanbul": "^3.2.4",
    "drizzle-kit": "^0.31.4",
    "globals": "^15.0.0",
    "pino-pretty": "^10.3.0"
  },
  "dependencies": {
    "@fastify/env": "^5.0.2",
    "@fastify/static": "^8.0.2",
    "dotenv": "^17.0.1",
    "drizzle-orm": "^0.44.2",
    "fastify": "^5.4.0",
    "fastify-plugin": "^5.0.1",
    "pg": "^8.16.3",
    "pino": "^9.7.0"
  }
}
