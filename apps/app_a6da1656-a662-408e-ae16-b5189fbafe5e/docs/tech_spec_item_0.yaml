client:
  components:
    entities:
    - description: Displays a single habit with its name and completion checkbox
      entity: habit
      name: HabitRow
      props:
      - habit
      - onToggle
    features:
    - description: Renders a list of HabitRow components with local state per session
      entities:
      - HabitRow
      feature: habit-list-ui
      name: HabitList
      props:
      - habits
      - onToggleHabit
      shared:
      - Card
    shared:
    - category: ui
      description: Accessible, styled checkbox component with visible focus and keyboard
        support
      name: Checkbox
      props:
      - checked
      - onChange
      - aria-label
    - category: ui
      description: Generic card container with shadow and padding
      name: Card
      props:
      - children
    - category: ui
      description: Accessible heading component for page titles and sections
      name: Heading
      props:
      - children
      - level
    widgets:
    - description: Combines header and habit list in a styled card layout
      entities:
      - HabitRow
      features:
      - HabitList
      name: HabitListCard
      props:
      - habits
      - onToggleHabit
      shared:
      - Card
      - Heading
  pages:
  - description: Allow users to view a list of habits and mark daily completion via
      an interactive interface.
    queries: []
    route: /habits
    title: Your Habits
    widgets:
    - HabitListCard
  - description: (Optional) Redirects to /habits as a single-page UI.
    queries: []
    route: /
    title: Redirect to Your Habits
    widgets: []
  stores:
  - actions:
    - toggleHabitCompletion
    - resetHabits
    description: Holds list of habits and their completion state for the current session
      (local only, resets on reload)
    layer: entities
    name: habitSessionStore
    state_shape:
    - name: habits
      type: Habit[]
components:
  parameters: []
  responses: []
  schemas:
  - name: Habit
    type: object
domains:
- apis: []
  description: Represents a habit and its local completion status for the current
    session.
  models:
  - description: Represents a user habit in the UI.
    fields:
    - name: id
      nullable: false
      pk: true
      type: string
    - name: name
      nullable: false
      pk: false
      type: string
    - name: completed
      nullable: false
      pk: false
      type: boolean
    name: Habit
  name: habit
  services:
  - description: Handles local state management of habits and their completion for
      the session. No persistence, no backend calls.
    methods:
    - getDefaultHabits
    - toggleHabitCompletion
    name: HabitSessionService
metadata:
  description: A frontend-only user interface for displaying a list of habits and
    marking daily completion, with no data persistence or backend.
  generated_at: '2024-06-12T12:00:00Z'
  name: Habit List UI
  version: 1.0.0
product:
  features:
  - description: Minimal, accessible habit tracker UI with local state only.
    domains:
    - habit
    id: habit-list-ui
    priority: high
    title: Habit List UI
  goals:
  - Provide an easy-to-use interface for viewing and checking off daily habits
  - Ensure accessibility for all users (keyboard, high contrast, visible focus)
  - No backend or persistent data storage; all state is ephemeral and local
  personas:
  - description: Any person who wants to quickly track daily habits in a simple, non-persistent
      way.
    goals:
    - View their daily habits
    - Mark habits as complete for today
    - Use the interface with keyboard, mouse, or touch
    name: General User
  summary: Minimal, accessible UI for users to view habits and mark completion for
    the day, with all state managed locally in the client.
use_cases:
- actors:
  - General User
  apis: []
  description: The user sees a card with a list of example habits and their completion
    state.
  domains:
  - habit
  happy_path:
  - User opens the Your Habits page.
  - A list of example habits is displayed with checkboxes/toggles.
  - No backend or data entry is required.
  id: view-habit-list
  pages:
  - /habits
  postconditions:
  - The user sees the list of habits and each habit's completion state for the session.
  preconditions:
  - User navigates to the Your Habits page.
  title: View Habit List
- actors:
  - General User
  apis: []
  description: The user marks a habit as complete or incomplete for the day.
  domains:
  - habit
  happy_path:
  - User clicks or uses keyboard on a checkbox/toggle.
  - UI updates local state and reflects checked/unchecked visually.
  id: mark-habit-complete
  pages:
  - /habits
  postconditions:
  - The visual state of the habit row updates as checked or unchecked.
  - No data is persisted; state resets on reload.
  preconditions:
  - Habit list is displayed.
  title: Mark Habit as Complete
user_journeys:
- id: basic-tracking-session
  persona: General User
  related_use_cases:
  - view-habit-list
  - mark-habit-complete
  steps:
  - action: Open the Your Habits page
    page: /habits
  - action: See the list of default habits
    page: /habits
  - action: Use mouse, touch, or keyboard to check one or more habits as complete
    page: /habits
  - action: See visual feedback for completion (checkbox checked, color change)
    page: /habits
  - action: Close or reload page; completion state resets
    page: /habits
  success_criteria: User can quickly check off habits for the day; completion state
    is obvious visually; no login or persistence required.
  title: Track Daily Habits (Session)
