plan_items:
- id: '1'
  name: Add toast message when item is marked as done
  sub_items:
  - effort: 1
    id: '1.1'
    name: Detect item marked as done event
    summary: Update the code to detect when a user marks an item as done (e.g., checkbox
      or button click).
  - effort: 2
    id: '1.2'
    name: Implement toast message component
    summary: Create or use an existing toast message component to display notifications.
  - effort: 1
    id: '1.3'
    name: Trigger toast on done event
    summary: Show the toast message when the item is marked as done, with appropriate
      success text.
  summary: Show a toast notification to the user when they mark an item as done in
    the UI.
